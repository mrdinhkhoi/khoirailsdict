<body>
<nav class="navbar navbar-default">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
      <span class="sr-only"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Rails dict", root_path, class: "navbar-brand", id: "logo" %>
  </div>
  <div class="collapse navbar-collapse" id="navbar-collapse-1">
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "Home", root_path, {:style=>"color:#fff !important;"} %></li>
      <% if logged_in? %>
        <% if current_user.admin? %>
              <li><%= link_to "Users", users_path, {:style=>"color:#fff !important;"} %></li>
              <li><%= link_to "New post", newp_path, {:style=>"color:#fff !important;"} %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="color:#fff">
            Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Profile", current_user, {:style=>"color:#fff !important;"} %></li>
            <li><%= link_to "Settings", edit_user_path(current_user), {:style=>"color:#fff !important;"} %></li>
            <li class="divider"></li>
            <li>
              <%= link_to "Log out", logout_path,{method: "delete", :style=>"color:#fff !important;"} %>
            </li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "Log in", login_path, {:style=>"color:#fff !important;"} %> </li>
      <% end %>
      <!-- <li id="smallsearch">
       <%= form_tag(posts_path, :method => "get", id: "search-form") do %>
          <div class="input-group" >
            <%= text_field_tag :search, params[:search],class: "form-control input-sm", 
                                placeholder:"" %>
              <span class="input-group-addon">
              <button class="btn btn-link btn-sm"><span class="glyphicon glyphicon-search"></span></button>
            </span>
          </div>
        <% end %>
      </li>  -->
    </ul>
  </div>
</nav>
</body>
